<div class="container">
  <h1>Add to Bookmark?</h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment, html5: true%>
    <%= f.submit "Submit", class: "btn btn-primary" %>
  <% end %>
  <div>
